---
import Layout from "@/layouts/Layout.astro";
import { solutions } from "@/config/solutions";
import { siteConfig } from "@/config/site";

const title = `حلول الأعمال في السعودية | زيادة المبيعات والأتمتة | ${siteConfig.name}`;
const description =
    "حلول لكل مشاكل عملك: زيادة المبيعات، رد تلقائي واتساب، الظهور في Google، متجر إلكتروني، توفير الوقت والتكاليف. خدماتنا في الرياض وكل السعودية.";
---

<Layout title={title} description={description}>
    <main class="min-h-screen bg-black text-white">
        {/* Hero */}
        <section class="relative py-20 overflow-hidden">
            <div
                class="absolute inset-0 bg-gradient-to-br from-purple-900/20 via-black to-blue-900/20"
            >
            </div>
            <div class="container mx-auto px-4 relative z-10">
                <div class="text-center max-w-4xl mx-auto">
                    <h1 class="text-4xl md:text-6xl font-bold mb-6">
                        حلول لكل <span
                            class="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-600"
                            >مشاكل عملك</span
                        >
                    </h1>
                    <p class="text-xl text-gray-300 mb-8">
                        شو المشكلة؟ عندنا الحل! اختر مشكلتك وشوف كيف نحلها
                    </p>
                </div>
            </div>
        </section>

        {/* Solutions Grid */}
        <section class="py-20">
            <div class="container mx-auto px-4">
                <div
                    class="grid md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6"
                >
                    {
                        solutions.map((solution) => (
                            <a
                                key={solution.id}
                                href={`/solutions/${solution.slug}`}
                                class="group bg-gradient-to-br from-gray-900 to-gray-800 rounded-2xl p-6 border border-gray-700 hover:border-purple-500/50 transition-all hover:scale-105"
                            >
                                <span class="text-4xl block mb-4">
                                    {solution.icon}
                                </span>
                                <h2 class="text-xl font-bold mb-2 group-hover:text-purple-400 transition-colors">
                                    {solution.titleAr}
                                </h2>
                                <p class="text-red-400 text-sm mb-2">
                                    ❌ {solution.problemAr}
                                </p>
                                <p class="text-green-400 text-sm">
                                    ✅ {solution.solutionAr.slice(0, 50)}...
                                </p>
                            </a>
                        ))
                    }
                </div>
            </div>
        </section>

        {/* CTA */}
        <section class="py-20 bg-gray-900/50">
            <div class="container mx-auto px-4 text-center">
                <h2 class="text-3xl md:text-4xl font-bold mb-4">
                    ما لقيت مشكلتك؟
                </h2>
                <p class="text-gray-400 mb-8">
                    تواصل معنا وخبرنا عن التحدي الذي تواجهه. عندنا حل لكل شي!
                </p>
                <a
                    href={`https://wa.me/${siteConfig.contact.whatsapp}?text=مرحباً، عندي مشكلة وأريد حل`}
                    class="inline-flex items-center gap-2 bg-gradient-to-r from-green-500 to-green-600 text-white px-8 py-4 rounded-full text-lg font-semibold hover:scale-105 transition-transform"
                >
                    تواصل معنا الآن
                </a>
            </div>
        </section>
    </main>
</Layout>
